SELECT MAX(LifeExpectancy)
FROM country;

SELECT COUNT(*)
FROM country;

SELECT COUNT(*)
FROM sakila.film;

SELECT SUM(Population)
FROM country;

SELECT AVG(Population) AS 'Promedio Total'
FROM country;

SELECT AVG(Population) AS Promedio
FROM country;

SELECT AVG(Population) Promedio
FROM country;

SELECT AVG(Population) 'Promedio Total'
FROM country;